# Alembic configuration for Helion.
# DATABASE_URL is read from the application config in env.py (not from here).

[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

[post_write_hooks]
# hooks = black
# black.type = console_scripts
# black.entrypoint = black
# Uncomment to run black on new migration files.
